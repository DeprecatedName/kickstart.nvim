# Neovim config

This is a personal Neovim config for experimenting with plugins.

Original kickstart.nvim information is in kickstart-readme.md

# Setup

```
git clone https://github.com/DeprecatedName/kickstart.nvim.git "${XDG_CONFIG_HOME:-$HOME/.config}"/nvim-experimental
alias nvim-exp='NVIM_APPNAME="nvim-experimental" nvim'
```
